@model IEnumerable<SOZ_project.Models.ReportModel>

@{
    ViewData["Title"] = "Lista osób zaginionych";
}

<h1>Lista Główna Osób Zaginionych</h1>

<p>
    <a asp-action="Create">Nowe Zgłoszenie</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastSeen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastSeen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @if(User.Identity.IsAuthenticated)
                @Html.ActionLink("Edytuj", "Edit", new {id=item.Id}) |
                @Html.ActionLink("Szczegóły", "Details", new {  id=item.Id }) |
                @if (User.Identity.IsAuthenticated)
                @Html.ActionLink("Usuń", "Delete", new {  id=item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
